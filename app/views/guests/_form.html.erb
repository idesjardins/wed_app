<%= semantic_form_for @guest do |f| %>
	<% f.inputs do %>
		<%= f.input :first_name %>
		<%= f.input :last_name %>
		<%= f.input :relation %>
		<%= f.input :wed_party %>
  	<%= f.submit %>
  <% end %>
<% end %>